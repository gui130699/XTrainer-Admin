"use client";
import { useEffect,useState } from "react";import { useRouter } from "next/navigation";import { loginAdmin } from "@/services/auth";import { useAdmin } from "@/components/providers";import { Button,Card } from "@/components/ui";import { AdminAppLogo } from "@/components/admin-app-logo";
export default function Login(){const router=useRouter(),{admin}=useAdmin(),[error,setError]=useState(""),[loading,setLoading]=useState(false);useEffect(()=>{if(admin)router.replace("/")},[admin,router]);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setError("");setLoading(true);const form=new FormData(event.currentTarget);try{await loginAdmin(String(form.get("email")),String(form.get("password")));router.replace("/");}catch(error){setError(error instanceof Error?error.message:"Não foi possível entrar.");}finally{setLoading(false)}}return <main className="login"><div className="login-brand" aria-label="XTrainer Admin"><AdminAppLogo size={68}/></div><Card><span className="eyebrow">ACESSO RESTRITO</span><h1>Painel Administrativo</h1><p>Use a conta definida como administradora no Firebase compartilhado.</p><form onSubmit={submit}><label>E-mail<input name="email" type="email" autoComplete="email" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p className="error">{error}</p>}<Button disabled={loading}>{loading?"VERIFICANDO...":"ENTRAR"}</Button></form></Card></main>}
