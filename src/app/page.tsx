"use client";
import { useEffect,useState } from "react";import Link from "next/link";import { AdminShell } from "@/components/admin-shell";import { Card,Loading,PageTitle } from "@/components/ui";import { dashboard } from "@/services/data";
type Metrics=Awaited<ReturnType<typeof dashboard>>;
export default function Dashboard(){const[metrics,setMetrics]=useState<Metrics|null>(null),[error,setError]=useState("");useEffect(()=>{dashboard().then(setMetrics).catch(()=>setError("Não foi possível carregar os indicadores."))},[]);return <AdminShell><PageTitle eyebrow="VISÃO GERAL" title="Painel administrativo" detail="Gerencie usuários cadastrados e a biblioteca de exercícios."/>{error&&<p className="error">{error}</p>}{!metrics?<Loading/>:<div className="metrics"><Card><span>Usuários cadastrados</span><strong>{metrics.users}</strong><Link className="text-button" href="/usuarios">Ver usuários</Link></Card><Card><span>Exercícios na biblioteca</span><strong>{metrics.exercises}</strong><Link className="text-button" href="/exercicios">Gerenciar exercícios</Link></Card><Card><span>Exercícios ativos</span><strong>{metrics.activeExercises}</strong><Link className="text-button" href="/exercicios">Abrir biblioteca</Link></Card></div>}<Card><h2>Escopo do administrador</h2><p>Este painel administra somente os perfis cadastrados e a biblioteca global de exercícios. Treinos, sessões, peso, evolução e avaliações pertencem ao próprio usuário no XTrainer.</p></Card></AdminShell>}
